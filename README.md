# Daily Digest Archive

Static historical archive for the Daily Digest, generated with [Eleventy](https://www.11ty.dev/) and deployed to Cloudflare Pages.

## Development

Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Build the site:

```bash
npm run build
```