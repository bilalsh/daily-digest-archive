{# One hatched gutter rail. Included twice, flanking the content column.
   Part of the flex row, so it spans the full page height with no fixed
   positioning and no arbitrary offset maths. #}
<span aria-hidden="true" class="hidden w-7 shrink-0 border-x border-zinc-800 bg-hatch md:block"></span>
